#Load JDK 8

FROM openjdk:8

# Adding jar file to docker container
ADD /target/movie-react-service-1.0.jar movier-react-service-app.jar

# Make PORT available
EXPOSE 8000

# Start the docker by running  jar file
ENTRYPOINT ["java", "-jar", "movier-react-service-app.jar"]