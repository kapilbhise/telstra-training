#Load JDK 8

FROM openjdk:8

# Adding jar file to docker container
ADD /target/movie-react-service-0.1.jar movie-app.jar

# Make PORT available
EXPOSE 8000

# Start the docker by running  jar file
ENTRYPOINT ["java", "-jar", "movie-app.jar"]