#Load JDK 17

FROM openjdk:17

# Adding jar file to docker container
ADD /target/movie-react-service-0.0.1-SNAPSHOT.jar movie-app.jar

# Make PORT available
EXPOSE 8100

# Start the docker by running  jar file
ENTRYPOINT ["java", "-jar", "movie-app.jar"]